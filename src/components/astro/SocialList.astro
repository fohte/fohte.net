---
import { socialLinks } from '#data/socialLinks'
---

<ul class="flex flex-col gap-2">
  {
    socialLinks.map((social) => (
      <li>
        <a
          href={social.href}
          class="text-foreground hover:text-primary inline-flex items-center gap-2 font-[family-name:var(--font-mono-ui)] text-sm no-underline transition-colors"
          target="_blank"
          rel={
            social.rel
              ? `${social.rel} noopener noreferrer`
              : 'noopener noreferrer'
          }
        >
          <span class="text-primary font-bold">&gt;</span>
          <span>{social.href.replace(/^https?:\/\//, '')}</span>
        </a>
      </li>
    ))
  }
</ul>
